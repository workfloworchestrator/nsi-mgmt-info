apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsi-mgmt-info.fullname" . }}-test-connection"
  labels:
    {{- include "nsi-mgmt-info.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nsi-mgmt-info.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
